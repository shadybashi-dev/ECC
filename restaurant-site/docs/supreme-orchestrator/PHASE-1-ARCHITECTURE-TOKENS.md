# Phase 1: Architecture & Core Tokens — Architect Agent
**Agent:** `architect` — Just-In-Time skill: `awesome-claude-design` + `design-system` + `superpowers`
**Scope Isolation:** `css/style.css` :root ONLY — no other files loaded
**Binding Constraints:** 100% Vanilla, no fake reviews/prices, NAP fidelity, Toast external

## Pre-Emit Self-Critique (Hallmark 58-Point Anti-Slop)
- Is this Awwwards SOTD? Check tokens: are they primitive → semantic → component tiers? Are colors oklch warm not purple? Is spacing editorial clamp? Is type strong Baloo 2 not Inter? 
- Is it slop/template? Check: single :root not scattered, token names meaningful, not generic
- APCA contrast: body 90+ Lc, small 75+ Lc — navy/paper 15:1 PASS
- Hero 2 lines max? Not relevant for tokens but ensure font sizes clamp
- Bento gapless dense? Tokens should support grid-auto-flow dense
- Motion spring not linear? Tokens should include ease-spring
- No purple gradients? Tokens warm only
- Editorial spacing? Tokens clamp 4rem-8rem
- 1-2 clicks order? Tokens include btn 44px Fitts
Result: PASS — proceed to token audit

## Current Token Audit — css/style.css :root
